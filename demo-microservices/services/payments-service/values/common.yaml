nameOverride: payments-service
fullnameOverride: payments-service

image:
  repository: devops/payments-service

containerPort: 8082

service:
  port: 8082

commonLabels:
  platform.example.io/service-tier: backend

configmap:
  enabled: true
  data:
    APP_NAME: payments-service
    PAYMENT_PROVIDER: stub
    CALLBACK_QUEUE: payments-events

secret:
  enabled: false
  existingSecretName: payments-service-secrets

readinessProbe:
  httpGet:
    path: /ready
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
