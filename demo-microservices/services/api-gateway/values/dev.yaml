image:
  tag: "0.1.0-dev.1"

configmap:
  data:
    LOG_LEVEL: debug

ingress:
  enabled: true
  hosts:
    - host: api-gateway.dev.platform.local
      paths:
        - path: /
          pathType: Prefix
