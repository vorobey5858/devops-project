controller:
  kind: DaemonSet
  hostNetwork: true
  dnsPolicy: ClusterFirstWithHostNet
  service:
    type: ClusterIP
  publishService:
    enabled: false
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false
